meta {
  name: AddItem BrighterDarker
  type: http
  seq: 3
}

post {
  url: http://localhost:5003/api/brighterdarker
  body: json
  auth: inherit
}

body:json {
  {
    "id": {
      "value": "1"
    },
    "name": "{{uuid}}",
    "quantity": 1
  }
}

script:pre-request {
  bru.setVar('uuid', require("uuid").v4());
  
}

settings {
  encodeUrl: true
  timeout: 0
}
