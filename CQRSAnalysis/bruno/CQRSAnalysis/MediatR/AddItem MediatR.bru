meta {
  name: AddItem MediatR
  type: http
  seq: 2
}

post {
  url: http://localhost:5003/api/mediatR
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{uuid}}",
    "quantity": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
